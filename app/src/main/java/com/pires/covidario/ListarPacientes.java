package com.pires.covidario;

import com.pires.covidario.Pacientes;

import java.util.ArrayList;

public class ListarPacientes {

    public ListarPacientes(){
        String[] columns = new String[]{
                "Nome do paciente: " ,
                "Data de nascimento: ",
                "Data de hoje: ",
                "Lugares que visitou: ",
        };

     /*   ArrayList<Pacientes> lista = ();

        Object[][] infos = new Object[lista.size()][7];

        for (int i = 0; i < lista.size(); i++) {
            Pacientes p = lista.get(i);
            infos[i][0] = p.getNome();
            infos[i][1] = p.getNascimento();
            infos[i][2] = p.getData();
            infos[i][3] = p.getLugares();
        }

*/
    }
}
